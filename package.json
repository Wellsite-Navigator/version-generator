{
  "name": "@wellsite/version-generator",
  "version": "1.0.0",
  "description": "Generates Versions based on git information",
  "repository": {
    "type": "git",
    "url": "git@wellsite.github.com:Wellsite-Navigator/version-generator.git"
  },
  "main": "index.js",
  "types": "index.d.ts",
  "files": [
    "dist"
  ],
  "bin": {
    "generate-version": "./dist/cli.js"
  },
  "scripts": {
    "test": "pnpm jest",
    "build": "rm -rf dist && npx tsc && chmod +x dist/cli.js",
    "clean": "rm -rf dist && rm -rf node_modules && rm -rf .coverage"
  },
  "packageManager": "pnpm@10.6.2",  
  "dependencies": {
    "commander": "^11.1.0",
    "glob": "^10.3.10",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/jest": "29.5.14",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.11.19",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2"
  }
  
}
